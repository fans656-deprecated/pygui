from pygui import *

text('hello world')
# we can't call text() multiple times to show multiple line of text
# cause do(text, 'hi') then will have accumulative effect
# text('easy python gui!')
